# Test fixture — deterministik agentlar ro'yxati (bats testlari uchun).
# Real config/agents.conf'dan MUSTAQIL: testlar bu faylga tayanadi, shunda
# repo'dagi agentlar o'zgarsa ham testlar buzilmaydi.
#
# • "Alpha CLI" binari = `bash` — har qanday tizimda MAVJUD (status ✓ deterministik).
# • "Bravo CLI" binari = mavjud bo'lmagan nom — status ✗ deterministik.
# • "Charlie"  — kategoriyasi BO'SH (DEFAULT_CATEGORY="AI" sinash uchun).
# • "NoInstall" — install/izoh maydonlari bo'sh (ixtiyoriy maydonlar sinash uchun).

Alpha CLI|bash|bash -c true|npm install -g alpha|Test agent A|Coding|🔑 ALPHA_API_KEY|https://example.com/alpha
Bravo CLI|definitely-not-real-xyz|bravo|python3 -m pip install bravo|Test agent B|Local|🆓 bepul — login shart emas|https://example.com/bravo
Charlie|charliebin|charlie run|bash -c "$(curl -fsSL https://example.com/i.sh)"|Kategoriyasiz agent||🌐 brauzer login|
NoInstall|nibin|nibin|||Bo'sh maydonli agent|Agent|💳 obuna|
