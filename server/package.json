{
  "name": "aidevix-stats-server",
  "version": "1.0.0",
  "description": "Aidevix CLI — global foydalanish statistikasi backend'i (Fastify + Redis).",
  "type": "module",
  "private": true,
  "main": "src/server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "@fastify/rate-limit": "^10.2.2",
    "fastify": "^5.2.1",
    "ioredis": "^5.4.2"
  },
  "devDependencies": {
    "ioredis-mock": "^8.9.0",
    "pino-pretty": "^13.0.0"
  }
}
